server:
  port: 8501

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zigbee?characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 826929


mybatis:
  #  引入配置文件
  config-location: classpath:/mybatis-config.xml
  #  设置资源路径
  mapper-locations: classpath:/Mapper/*/*.xml
